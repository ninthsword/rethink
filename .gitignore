node_modules
*.key
*.pem
*.cert
*.sw*
*.csr
*.crt
dist
oauth.json
state
# 'state' above protects bridge.storage_path, whose device certificates cannot be
# rebuilt without the appliance.
captures/

# 작업 지시서와 로컬 AI 도구 상태 (개인 정보 포함)
CLAUDE.md
CLAUDE.local.md
AGENTS.md
.claude/
