node_modules
*.key
*.pem
*.cert
*.sw*
*.csr
*.crt
dist
oauth.json
state
# 'state' above protects bridge.storage_path, whose device certificates cannot be
# rebuilt without the appliance. It also matched .claude/state/, which is project
# knowledge meant to be committed.
!.claude/state/
captures/
.claude/worktrees/
